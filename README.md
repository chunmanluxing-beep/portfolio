# 产品作品集 · 静态站

一个产品经理作品集单页,介绍「榴档期 · 旅拍自动化调度管理系统」这套真实运行的生产系统——它给谁用、两个端怎么分工、一条订单怎么走完、以及几个关键的产品决策。

## 这是什么

我在日本做旅拍业务,自建了一套覆盖「接单 → 建单 → 派单 → 拍摄 → 交付 → 结算」全链路的业务系统,由三个端协同:运营总控台(管理端)、摄影师移动端「榴档期 App」(iOS 已上架 App Store)、客人免登录交付页。

站点按**先总后分**组织:

- 先说给谁用(四类使用者),再说系统由哪两个入口构成
- 用一张「一条订单的完整旅程」把全站串起来,后面每一块能力都挂在这条线的某一段上
- 十二个能力板块**按归属分成三组**(管理端专属 / 两端共用 / 摄影师端专属),每张卡片可点开看图文详情
- 榴档期 App 的十二项差异化做成**图标 + 一句话**的网格,想看完整论证再点开
- 密度控制:九项机制留三条、七条优势留三条、四个决策只露标题与一句痛点一句结果,其余默认折叠但一条没删

## 页面结构(十一屏)

```
 1. Hero          Logo 占位 + 品牌名 + 四个可点开的 KPI
 2. 这是什么系统   三问三答 + 系统全景动画流程图(配图0:来源 → 三端 → 数据底座,订单圆点沿线流动)
 3. 适用场景      四类使用者各自拿到什么(配图1)
 4. 系统构成      同一条订单的两端镜像(配图2)+ 一条订单的完整旅程(配图3,七步全部可点开)★全站地图
 5. 管理端        十二块能力分三组(每块展开有优势数据条)· 建单流程对比(配图5)
 6. 派单调度      派单模式对比(配图6)+ 三条关键机制 + 抢单动线(配图7)+ 四个可点开的 KPI
 7. 榴档期 App    三块主界面 + 抢单大厅示意(配图8)+ 十二项差异化图标网格
 8. 模式优势      八环节对比表 + 交付交接对比(配图9)+ 三条优势(另四条折叠)
 9. 自动化与 AI   人工介入次数对比(配图10)+ 自动化清单 + AI 边界
10. 产品决策      四个案例(默认折叠,含事故影响面图解 配图11)
11. 如何交付      无研发团队的人机协作方法论
```

顶部导航 7 项:适用场景 / 系统构成 / 管理端 / 派单调度 / 榴档期 App / 模式优势 / 决策与交付。

## 配色:暖色琥珀

深色底 + 琥珀橙强调色,全部走 CSS 变量,改一处即可全站生效:

```
--bg #12100C   --bg-alt #16130E   --bg-deep #0D0B08
--accent #E8A33D   --accent-2 #F2BC63          琥珀主色 / 浅琥珀
--warn #C97B4A     --photo #A0AB5E             暖砖红(客人)/ 暖橄榄(摄影师端)
```

三个角色色在流程图与标签上通用:管理端=琥珀,摄影师端=橄榄,客人=砖红。

端的划分按**角色权限**而非设备:运营角色从电脑浏览器或手机上的榴档期 App 都能进入总控台;摄影师角色登录看到的是接单工作台。

## 可展开的图文详情

全站共 53 处原生 `<details>`,展开收起不依赖任何脚本:

- **八个 KPI 数字**(Hero 四个 + 派单调度四个)——点开是「传统模式的代价 → 这个数字意味着什么 → 传统 vs 本系统对比条」,统计口径(含「待生产库核验」的诚实标注)收在内层「统计口径」折叠里,最下方是佐证截图位
- **订单旅程七步**(配图3)——每一步点开看系统具体做了什么、一张微型示意图、跳往对应专章的「详细 →」
- 十一张管理端能力卡(展开顶部有优势数据条)
- 十二格 App 差异化能力
- 四个产品决策
- 两处「更多机制 / 更多优势」折叠区、一处「三端业务流」折叠图

整张卡片任意位置都可点击(图标、标题、描述、要点都在 `summary` 内且不拦截点击),右上角 `+` / `−` 指示,KPI 卡右下角是「看口径 ＋」,展开时 `＋` 旋转 45° 成 `×`;键盘 Tab + 回车同样可用。

## 动效

全部动效**统一包在 `@media (prefers-reduced-motion: no-preference)` 内**,基础层不设 `opacity:0`——
**关闭动效的用户看到的是完整静态内容,不会因为动效缺失而丢失任何信息**。

| 动效 | 位置 | 说明 |
| --- | --- | --- |
| 三层依次淡入上浮 | 配图 0 系统全景 / 三问三答 | 一次性播放,`animation-fill-mode: both` 停在终态 |
| 字段映射连线流动 | 配图 2 两端镜像 | 4.2s / 5.4s / 6.5s 慢速循环,不抢注意力 |
| 展开内容淡入下滑 | 全部 `details[open]` | 0.32s,配合 `＋` 旋转 |
| 配图轻量入场 | 12 张 CSS 配图 | 淡入 + 微上浮,不改动图形结构 |
| 订单圆点沿线流动 | 配图 0 系统全景 | 4.5s / 5.5s 循环,数据底座另有 7s 流光 |
| KPI 对比条展开 | 8 个 KPI 展开区 | 条形从 0 宽展开到目标宽,0.6s |
| 环节箭头流动 | 图 4 三端业务流 | 3.6s 循环渐变 |

## 二十四张配图全部由 CSS 绘制

12 张主配图(含配图 0)+ 12 张板块机制图 + 12 个板块图标 + 12 个差异化图标 + 4 个角色图标,全部用 `div/span` + `flex/grid` + `border` + `border-radius` + 伪元素实现。不使用图片文件、图标字体、canvas 或 SVG,因此页面没有任何图片请求。

## 如何替换 Logo

导航栏与 Hero 各留了一个 Logo 占位(`.logoslot` 36×36、`.logoslot.big` 72×72,虚线框内写着 LOGO):

1. 把 logo 文件放进仓库根目录 `images/logo.png`(建议 512×512 透明底 PNG,深色底上要看得清)。
2. 把两处占位替换成 `img`:

```html
<!-- 替换前 -->
<span class="logoslot" aria-hidden="true">LOGO</span>
<!-- 替换后 -->
<img class="logoimg" src="images/logo.png" alt="榴档期">
```

3. 在 `style.css` 末尾补上尺寸:

```css
.logoimg      { display: block; width: 36px; height: 36px; border-radius: 9px; object-fit: contain; }
.logoimg.big  { width: 72px; height: 72px; border-radius: 16px; margin: 0 0 22px; }
```

Hero 那个记得加 `class="logoimg big"`。

## 如何替换截图占位

页面里预留了 **36 处**真实截图占位区(虚线框),去重后需要 **32 张图**。

> **完整清单见 [`images/README.md`](images/README.md)** ——每一处占位对应哪个文件名、放什么截图、在哪一屏、建议尺寸、必须打码什么,都列好了,并按优先级分组列出。页面上每个占位框的第二行也直接写着它对应的文件名。

### 放图即显示(不需要改任何代码)

页面里 36 处占位已经全部预接好 `<img>`:先找 `.png`,找不到自动找同名 `.jpg`,两种都没有才回落成虚线占位框。

1. **先打码**——客人姓名、电话、邮箱、金额、订单号、网盘提取码等敏感信息必须处理干净再入库。图片一旦提交进 Git 并推上 GitHub,历史里就永远存在,删掉当前版本也没用。
2. 照着 [`images/README.md`](images/README.md) 的文件名(全小写、连字符、`.png`),把图放进 `images/`。
3. 刷新页面——**放进去就显示了,`index.html` 一行都不用改**。
4. 想让它出现在线上,`git add images/xxx.png` 后提交推送即可。

回落机制(每处占位的实际结构):

```html
<figure class="shotwrap">
  <img class="shotimg" src="images/console-orders-list.png" alt="总控台订单列表页"
       onerror="this.style.display='none';this.nextElementSibling.style.display='flex'">
  <div class="shot">
    <p class="shot-name">总控台订单列表页截图</p>
    <p class="shot-req">建议文件名:console-orders-list.png ｜ 建议 1600×900 ｜ 需打码:客人姓名、电话、金额</p>
  </div>
</figure>
```

- `.shotwrap > .shot` 在 CSS 里默认 `display:none`,只有 `onerror` 触发时才置回 `flex`
- 图片加载失败时 `<img>` 自身立即 `display:none`,**不会出现浏览器默认的破图图标**
- 卡片内的 31 处用 `.shotwrap-in`,图片最大高度 420px;主线 5 处 520px

占位框不替换也不影响观感——它本身是按设计元素处理的,留着即可。

## 本地预览

不需要构建工具、不需要安装依赖:

```bash
open index.html                 # 直接打开
python3 -m http.server 8000     # 或起本地服务,访问 http://localhost:8000
```

## 技术说明

```
index.html    单页全部内容(十一屏 + 二十四张 CSS 配图 + 二十八个 CSS 图标)
style.css     暖色深色样式,响应式断点 1024px / 768px / 380px,含动效层与打印浅色适配
images/       截图目录:按清单文件名放图即自动显示;README.md 是 36 处占位的完整清单
.nojekyll     关闭 GitHub Pages 的 Jekyll 处理
```

- **无外部 JS 文件;仅 36 处 `<img onerror>` 内联回退(png → jpg → 占位框两级),用于截图缺失时显示占位框**
- 平滑滚动由 CSS `scroll-behavior` 实现,板块展开由原生 `<details>` 实现
- 零第三方资源:系统字体栈,不引任何 CDN;截图放进 `images/` 后即为本仓库自有图片
- 支持打印导出 PDF

## 数据说明

页面中的业务数字均取自 2026-08 生产环境的只读查询,未做估算或推演。两处例外已在页面上明确标注口径:

- 建单流程对比中的耗时与人力,是 300 单规模下的实际运营口径;其中「2–4 分钟」只指**建单环节本身**,与派单侧的接单时长中位数 2.0 分钟是两个独立指标,不叠加。
- 人工介入次数对比图中的「≈ 15 次」与「≈ 4 次」为**示意口径**,按主要操作节点计数,用于体现结构差异,不是精确统计。

## 部署

托管于 GitHub Pages(main 分支根目录)。
