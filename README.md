# 产品作品集 · 静态站

一个产品经理作品集单页,介绍「榴档期 · 旅拍自动化调度管理系统」这套真实运行的生产系统——它给谁用、两个端怎么分工、一条订单怎么走完、以及这套系统是怎么交付出来的。

## 这是什么

我在日本做旅拍业务,自建了一套覆盖「接单 → 建单 → 派单 → 拍摄 → 交付 → 结算」全链路的业务系统,由三个端协同:运营总控台(管理端)、摄影师移动端「榴档期 App」(iOS 已上架 App Store)、客人免登录交付页。

站点按**先总后分**组织:

- 先说给谁用(四类使用者),再说系统由哪两个入口构成
- 用一张「一条订单的完整旅程」把全站串起来,**七步全部是可点目录**,点哪一步就跳到讲它的那一章
- 能力按**双主线**编排:屏 3「运营总控台」四块(建单 / 派单 / 交付 / 结算)、
  屏 4「榴档期 App」四块(档期 / 抢单 / 预警 / 收入),每一块都是主线上的一段
- 密度控制:深论证收进 `<details>`,默认折叠但一条没删

## 页面结构(七屏)

```
1. Hero            #top      品牌名 + 一句话 + 双视角演示影片(客人篇 / 客服篇)
                             + 证据条 5 项 + 可点开的 KPI
2. 全景与目录       #map      系统全景(图 0)· 双端界面并排(图 1)
                             · 四类使用者各自拿到什么(图 2)· 全站目录(图 3,七步全部可点)
3. 运营总控台       #console  ★主线一★ 四块:建单(图 4)/ 派单(图 5、图 6)
                             / 交付(图 7)/ 结算
4. 榴档期 App      #app      ★主线二★ 四块:档期 / 抢单大厅(图 8)/ 预警推送 / 收入账单
5. 模式优势         #why      全链路对比表 + 人工介入次数对比(图 9)
6. 数字与口径       #data     KPI 与统计口径
7. 如何交付         #how      无研发团队的人机协作方法论
```

顶部导航 7 项,与七屏一一对应:
概览 / 全景与目录 / 运营总控台 / 榴档期 App / 模式优势 / 数字与口径 / 如何交付。

全站主配图 **10 张,编号 图 0–图 9 连续**,且**编号顺序与浏览顺序一致**。

## 配色:暖色琥珀

深色底 + 琥珀橙强调色,全部走 CSS 变量,改一处即可全站生效:

```
--bg #12100C   --bg-alt #16130E   --bg-deep #0D0B08
--accent #E8A33D   --accent-2 #F2BC63          琥珀主色 / 浅琥珀
--warn #C97B4A     --photo #A0AB5E             暖砖红(客人)/ 暖橄榄(摄影师端)
```

三个角色色在流程图与标签上通用:管理端=琥珀,摄影师端=橄榄,客人=砖红。

端的划分按**角色权限**而非设备:运营角色从电脑浏览器或手机上的榴档期 App 都能进入总控台;摄影师角色登录看到的是接单工作台。

## 可展开的图文详情

全站共 **33 处**原生 `<details>` + **8 组**纯 CSS radio 多图轮播 + **36 处**纯 CSS checkbox 图片灯箱
(点击任意截图全屏放大,点暗底或 ✕ 关闭),展开收起、轮播与放大**全部不依赖任何脚本**:

- **六个 KPI 数字**——点开是「传统模式的代价 → 意味着什么 → 对比条」,统计口径(含「待生产库核验」标注)收在内层折叠
- **图 3 订单旅程七步**——每一步整体是页内锚点,直接跳到讲它的那一章
- **屏 3 / 屏 4 每块的深论证**——「为什么这是结构变化」这类长论证一律收进折叠

整张卡片任意位置都可点击;键盘 Tab + 回车同样可用(轮播以隐藏 radio 承接焦点)。

## 动效

全部动效**统一包在 `@media (prefers-reduced-motion: no-preference)` 内**,基础层不设 `opacity:0`——
**关闭动效的用户看到的是完整静态内容,不会因为动效缺失而丢失任何信息**。

| 动效 | 位置 | 说明 |
| --- | --- | --- |
| 三层依次淡入上浮 | 图 0 系统全景 / 三问三答 | 一次性播放,`animation-fill-mode: both` 停在终态 |
| 字段映射连线流动 | 图 1 两端镜像 | 4.2s / 5.4s / 6.5s 慢速循环,不抢注意力 |
| 展开内容淡入下滑 | 全部 `details[open]` | 0.32s,配合 `＋` 旋转 |
| 配图轻量入场 | 10 张 CSS 主配图 | 淡入 + 微上浮,不改动图形结构 |
| 订单圆点沿线流动 | 图 0 系统全景 | 4.5s / 5.5s 循环,数据底座另有 7s 流光 |
| KPI 对比条展开 | 6 个 KPI 展开区 | 条形从 0 宽展开到目标宽,0.6s |
| 环节箭头流动 | 图 6 抢单动线 | 3.6s 循环渐变 |
| 订单一生全景动画 | Hero | 圆点流经六站 10.5s/周期,到站亮起 + 状态小字浮现,结算站定格 1.5s;reduced-motion 时六站全亮、状态字全显 |
| 总控台↔App 桥接线 | 图 0 | 两条 L 形线锚定两节点底边,标签「派单下发 →」「← 档期 · 接单回写」贴线;圆点对向流动 4s |
| 角色卡前后对比图 | 图 2 四卡 | 一次性淡入上浮 |
| 串行爬点 vs 并行脉冲 | 图 5 派单对比 | 左点在「未回复/没空」处停顿,右侧脉冲后依次点亮,7s |
| 交付页进度填充 | 图 7 | 一次性 1.6s,播完停在「选片」段 |
| 首屏双视角影片 | Hero 客人篇 / 客服篇 | 各约 14s 无缝循环,纯 CSS 模拟操作,右上角可暂停 |

## 全部配图由 CSS 绘制

**10 张主配图(图 0–图 9)** + 10 张板块机制图 + 4 幅角色卡「之前 → 之后」对比图
+ 4 个角色图标 + Hero 全景动画 + 首屏两部演示影片,全部用 `div/span` + `flex/grid` + `border`
+ `border-radius` + 伪元素实现,**不使用图标字体、canvas 或 SVG**。

> 注意:站内另有 **33 张真实界面截图**挂在 36 个图位上(见 `images/README.md`)。
> 那些是 `images/` 里的照片素材,**不属于**上面说的「CSS 配图」——页面是有图片请求的。

## 如何替换 Logo

导航栏与 Hero 各留了一个 Logo 占位(`.logoslot` 36×36、`.logoslot.big` 72×72,虚线框内写着 LOGO):

1. 把 logo 文件放进仓库根目录 `images/logo.png`(建议 512×512 透明底 PNG,深色底上要看得清)。
2. 把两处占位替换成 `img`:

```html
<!-- 替换前 -->
<span class="logoslot" aria-hidden="true">LOGO</span>
<!-- 替换后 -->
<img class="logoimg" src="images/logo.png" alt="榴档期">
```

3. 在 `style.css` 末尾补上尺寸:

```css
.logoimg      { display: block; width: 36px; height: 36px; border-radius: 9px; object-fit: contain; }
.logoimg.big  { width: 72px; height: 72px; border-radius: 16px; margin: 0 0 22px; }
```

Hero 那个记得加 `class="logoimg big"`。

## 如何替换截图占位

页面里预留了 **36 处**真实截图占位区(虚线框),去重后需要 **32 张主图**(其中 23 张已到位、9 张待重截/待补)。

> **完整清单见 [`images/README.md`](images/README.md)** ——每一处占位对应哪个文件名、放什么截图、在哪一屏、建议尺寸、必须打码什么,都列好了,并按优先级分组列出。页面上每个占位框的第二行也直接写着它对应的文件名。

### 放图即显示(不需要改任何代码)

页面里 36 处占位已经全部预接好 `<img>`:先找 `.png`,找不到自动找同名 `.jpg`,两种都没有才回落成虚线占位框。

1. **先打码**——客人姓名、电话、邮箱、金额、订单号、网盘提取码等敏感信息必须处理干净再入库。图片一旦提交进 Git 并推上 GitHub,历史里就永远存在,删掉当前版本也没用。
2. 照着 [`images/README.md`](images/README.md) 的文件名(全小写、连字符、`.png`),把图放进 `images/`。
3. 刷新页面——**放进去就显示了,`index.html` 一行都不用改**。
4. 想让它出现在线上,`git add images/xxx.png` 后提交推送即可。

回落机制(每处占位的实际结构):

```html
<figure class="shotwrap">
  <input type="checkbox" id="lb-N" class="lb-toggle">          <!-- 灯箱开关(纯 CSS) -->
  <label class="lb-zoom" for="lb-N">
    <img class="shotimg" src="images/console-orders-list.png" alt="总控台订单列表页"
         onerror="…先试同名 .jpg,再隐藏并显示占位框…">
    <span class="lb-tip">⤢ 点击放大</span>
  </label>
  <div class="shot">…占位框…</div>
  <div class="lb-overlay">                                     <!-- 点击后的全屏大图 -->
    <label class="lb-close" for="lb-N">✕</label>
    <label class="lb-back" for="lb-N"><img src="同一文件" loading="lazy"></label>
  </div>
</figure>
```

- `.shotwrap > .shot` 在 CSS 里默认 `display:none`,只有 `onerror` 触发时才置回 `flex`
- 图片加载失败时缩略区自动隐藏,**不会出现浏览器默认的破图图标**,占位框也不会带灯箱
- **每张已显示的截图都可点击放大**:`checkbox:checked ~ .lb-overlay` 全屏呈现同一文件(缓存命中,零额外流量),点暗底任意处或右上角 ✕ 关闭
- 卡片内的 31 处用 `.shotwrap-in`,图片最大高度 420px;主线 5 处 520px

占位框不替换也不影响观感——它本身是按设计元素处理的,留着即可。

## 本地预览

不需要构建工具、不需要安装依赖:

```bash
open index.html                 # 直接打开
python3 -m http.server 8000     # 或起本地服务,访问 http://localhost:8000
```

## 技术说明

```
index.html    单页全部内容(七屏 + 10 张 CSS 主配图 + 10 张板块机制图 + 2 部首屏影片)
style.css     暖色深色样式,响应式断点 1024px / 769px(展开态 KPI 横跨) / 768px / 480px / 380px,含动效层与打印浅色适配
images/       截图目录:按清单文件名放图即自动显示;README.md 是 36 处占位的完整清单
.nojekyll     关闭 GitHub Pages 的 Jekyll 处理
```

- **无外部 JS 文件;仅 82 处 `<img onerror>` + 4 处 `<img onload>` 内联回退(37 处缩略 + 37 处灯箱大图,png → jpg → 占位框两级;另 4 处轮播第 2 张的 onerror/onload 用于「有第 2 张才亮圆点」)**
- 平滑滚动由 CSS `scroll-behavior` 实现,板块展开由原生 `<details>` 实现,图片放大由纯 CSS checkbox 灯箱实现
- 零第三方资源:系统字体栈,不引任何 CDN;截图放进 `images/` 后即为本仓库自有图片
- 支持打印导出 PDF

## 数据说明

页面中的业务数字均取自 2026-08 生产环境的只读查询,未做估算或推演。两处例外已在页面上明确标注口径:

- 建单流程对比中的耗时与人力,是 300 单规模下的实际运营口径;其中「2–4 分钟」只指**建单环节本身**,与派单侧的接单时长中位数 2.0 分钟是两个独立指标,不叠加。
- 人工介入次数对比图中的「≈ 15 次」与「≈ 4 次」为**示意口径**,按主要操作节点计数,用于体现结构差异,不是精确统计。

## 部署

托管于 GitHub Pages(main 分支根目录)。

---

## v15.0 新增:竖图容器 / 多图轮播 / 手机端适配

### 一、竖图专用容器 `.shot-portrait`

摄影师端(App)截图是 **780×1688 手机竖图**,塞进按横图设计的 `width:100%` 容器里
会被 `object-fit:contain` 压成「宽框里的一根细条」,两侧留大片空底。v15 给这 10 处
(9 处已有图 + 1 处 B 类占位 `app-urgent-push`)加了 `shot-portrait` 类:

- 图片按自然比例收窄居中:桌面 `max-width:300px`、`max-height:640px`;窄屏 `min(300px,78vw)` + `max-height:72vh`
- 圆角 18px + 1px 浅边 + 投影,做出手机壳观感
- **无图态占位框同步改成竖比例虚线框(9:19.5)**,不再「横框等竖图」
- 灯箱内竖图:高度上限仍是 92vh,宽度按 780:1688 反推封顶(`min(94vw,44vh)`),不会出现超宽拉伸

### 二、多图轮播 `.shotdeck`(一个占位挂多张分截图)

**文件名约定**:主图 `<名>.png|.jpg`,追加图 `<名>-2` / `<名>-3` / `<名>-4`,**最多 4 张**。
把分开截的几张按这个序号丢进 `images/` 即可,不需要改 HTML。

- 纯 CSS:每张图对应一个隐藏 `input[type=radio]`(`name` 按占位唯一分组)+ 图下圆点 `label` 行,`:checked` 决定显示哪张
- **渐进增强**:圆点行默认隐藏,只有第 2 张真的加载成功(`onload` 打上 `.deck-multi`)才出现——
  只有主图时零圆点,外观与 v14 逐像素一致
- 圆点:视觉 8px(当前页琥珀实心、其余描边),触控热区 44×44
- **灯箱联动**:radio 与 `.lb-overlay` 同为 `figure` 直接子元素,用 `~` 共享 `:checked` 状态,
  放大看到的就是当前选中的那一张,不需要任何脚本
- v15 已为 4 处已知会有多张的占位预置了 2 图结构:`guest-photo-select`(×2 处)、`guest-delivery-page`(×2 处);
  其余 33 处保持单图结构不动

### 三、B2 撤销

「新版本更新提示」那一处占位整块删除(占位数 38 → 37),对应文件名一并从两份清单里摘除。
「版本自动感知」这块能力本身**保留、文字一字未动**——能力是真的,只是不配图。

### 四、手机端适配(第一验证视口 390×844)

- 触控热区:顶部导航 41×25 → 45×47;「统计口径」summary 39 → 44;
  正文内联链接(App Store 外链 / 「详细 →」/ 「专章 →」/ 邮箱)用不可见 `::after` 扩到 44px(版式一字不动);
  灯箱关闭键 40 → 46,并把 padding 从 overlay 挪进 `.lb-back`,使「点暗底任意处即关」覆盖到边缘
- 灯箱大图 `max-height` 加 `dvh` 兜底(`92vh` → `92dvh`),手机地址栏收起/展开不再错位裁切
- **新增 480px 断点**:v14 的单列断点停在 380px,而 390 是最常见的 iPhone 逻辑宽度——
  `.stats` 在 390 仍是两列(卡内截图被压到 90px 宽)、`.kpis` 在任何宽度都不塌成单列(116px 宽)。
  ≤480px 一律单列后,卡内截图回到 272–300px 宽

---

## v16.0 修正:清除错配截图 + 图位搬家 + 删除决策屏

上一轮做了一次全站审计,把每一处占位配的图拿去和「它的采集来源 / 画面实际内容」逐一对照,
查出 8 处错配。本轮按审计结论逐条改掉。

### 一、删除 4 张作废截图

| 文件 | 作废原因 |
|---|---|
| `guest-photo-select.jpg` | 画面是「加载失败 / 未登录或登录已过期」的**报错页**——它采自一条后端已无对应接口的老路由 |
| `app-schedule-fill.jpg` | 与 `app-calendar-weather.jpg` **md5 完全相同**,且画面被「选择城市」弹层全屏遮挡 |
| `app-calendar-weather.jpg` | 同上,日历与天气底色一点没露出来 |
| `guest-delivery-page.jpg` | 画面停在进度 14% 的早期阶段(既无底片也无成品),而且脱敏引擎把进度条的步骤名误替换成了人名 |

这 6 处挂载(含复用)自动回落成虚线占位框,等重截。**app 类的占位框是竖比例,console/guest 类是横比例**,形状与将来的图一致。

### 二、图位搬家(端别归位)

| 原位置 | 原因 | 新位置 |
|---|---|---|
| App 专章「四 · 评分梯队」挂 `console-tier-setting` | 这是**运营总控台**的界面;摄影师端根本没有梯队配置页,梯队只体现为「没到点就看不见这个单」 | 移到派单专章「评分梯队分层放号」下方,并在原格补一行说明 + 锚点跳转 |
| App 专章「六 · 推送文案」挂 `console-push-copy` | 同样是总控台界面;摄影师只收到推送本身,看不到文案模板 | 并入管理端专章「推送体系」,与 `console-push-center` 组成**两图轮播**(文件更名为 `console-push-center-2.jpg`) |
| 派单专章「三方联动自动下架」挂 `console-grab-modal` | 格子讲「被抢/改指派/取消 → 自动下架」,配的却是**发起**弹窗,画面里没有下架 | 改挂新占位 `console-grab-offdown`,等重截(**v18-A 已由工作台 D6 补齐**) |

### 三、据实修正的说法(全部有源码依据)

| 原文 | 改为 | 依据 |
|---|---|---|
| 镜像表「**派生** ／ 由订单状态推导生成」 | 「**实时判定** ／ 每次刷新当场算:任务还开着 + 这个拍摄项还没派人 + 我在候选名单里 + 轮到我这一档了 —— 四条都成立才是可抢」 | 摄影师端列表接口的真实过滤条件就是这四条,与「订单状态」不是一回事 |
| 图 8「灰卡可点击,弹出说明:提升评分可获得更多专属邀约」 | 「抢不了的单照常显示为灰卡,报酬与城市不遮挡,只标注『暂不可抢』」 | 该弹层文案在 App 源码里零命中;真实灰态只有一个不可点的「暂不可抢」 |
| 图 2「同一条订单的两端镜像」 | 「双端界面并排(左:总控台订单列表 / 右:摄影师端抢单大厅,两侧均为真实截图,**非同一条订单**)」 | 该图是手工拼的两窗截图,左右并不是同一条订单 |

### 四、删除自省性内容

- **删除整屏**:原第 10 屏「产品决策 · 四个关键决策」(含决策一至四、事故复盘与图 12)。**全站屏数 12 → 11**,图号变为 图 0–图 11 连续。
- 「只认终端实证」一条里的事故叙述删除,只留方法论;「曾有四个真机问题,自动化检查全绿」改为正向表述。
- 决策屏里唯一有据的数据(**抢单上线两个月即承接三分之一派单量 32.9%**)保留,搬进派单专章正文。
- 顶部导航第 7 项由 `#cases 决策与交付` 改为 `#how 交付方式`——原锚点随屏消失,而末屏本身要留,所以改指而不是删项。

### 五、展开态 KPI 卡横跨整行(桌面 ≥769px)

审计实测:1280 视口下 KPI 卡内截图只有 **180px 宽(缩放 12.8%)**,后台界面缩到这个尺寸等于噪点。
现在**只在这张卡被点开时**让它横跨整行:卡内图位 → **958×520 盒 / 可见画面 809px 宽(57.8%)**,提升 4.5 倍;
**收起态布局一字不动**(实测仍是 4 列 × 245.5px,相邻未点开的卡仍是 246px)。

---

## v16.1 手动截图入库(第一批)

### 本轮入库 4 张(全部为手机真机竖屏,来自摄影师端 App)

| 入库文件名 | 内容 | 像素 | 体积 |
|---|---|---|---|
| `app-grab-hall-2.jpg` | 抢单大厅 · 单张可抢卡(定向邀请单) | 828×1688 | 235 KB |
| `app-grab-hall-3.jpg` | 抢单详情页(报酬 / 拍摄地 / 进店 / 人数 / 距离 / 拍摄日天气) | 845×1688 | 166 KB |
| `app-grey-explain.jpg` | 已结束灰卡 · 「已派出 / 已被安排 / 需★≥5.0」 | 821×1688 | 278 KB |
| `app-grey-explain-2.jpg` | 灰卡详情 · 信息照给,按钮为「已被安排」 | 820×1688 | 275 KB |

### 由此产生的两处多图轮播

- **「二 · 抢单大厅」升级为三图轮播**:`app-grab-hall`(整页)→ `-2`(可抢卡)→ `-3`(抢单详情)。
  这也是**首次用满 3 张**,顺带把轮播样式从 2 张扩到约定上限 **4 张**(第 3/4 颗圆点只在对应图真的加载成功时才出现)。
- **「三 · 可见性与可抢性分离」新增一处两图轮播**:`app-grey-explain` + `-2`。

### 三处占位改为竖图容器

`guest-photo-select`(用于两处)与 `guest-delivery-page` 的实拍是**手机竖图**,
对应的 lb-17 / lb-36 / lb-38 三处已加 `shot-portrait`,占位框同步变成竖比例(实测 300×640 桌面 / 300×608 手机)。
全站竖图位由 10 处增至 **14 处**。

### 统计

| 项 | v16.0 | v16.1 |
|---|---|---|
| 图片文件 | 24 | **28** |
| 占位处数 | 36 | **37** |
| 显示 / 空位 | 26 / 10 | **27 / 10** |
| 灯箱接线 / 灯箱大图 | 36 / 41 | **37 / 45** |
| 多图轮播 | 5 处 | **7 处**(其中 1 处 3 图) |

---

## v16.2 客人交付页据实重写 + 灰卡描述回改

用户口述了三条实机事实,其中两条推翻了此前依据源码做出的推断。**涉及交互行为的结论一律以实机为准**——源码能证明「某句文案不存在」,证明不了「某个交互不存在」。

### 一、客人选片:不是点选照片,是填照片编号

真实机制(已由用户实拍截图与源码双向印证):客人拿到**底片的网盘链接**后,在交付页的输入框里**填要精修的照片编号**(逗号分隔,可填多个,输入框上限 5000 字),提交即自动通知下一棒。系统对编号不生成、不校验、不比对——编号来自网盘里的文件名。

| 位置 | 改前 | 改后 |
|---|---|---|
| #whatis 第三端描述 | 进度自查、**自助选片**、一键验收 | 进度自查、**按编号提交精修需求**、一键验收 |
| #who 收益句 | 免登录进度页,**自助选片**与验收 | 免登录进度页,**填编号提交精修需求**与验收 |
| #system 能力卡(标签) | **自助选片**与验收返修 | **填编号提交精修**与验收返修 |
| #system 能力卡(要点) | 客人**自助选片**、验收与提返修 | 客人**填编号提交精修需求**、验收与提返修 |
| #guest 收益清单 | **自助选片**,提交即自动通知下一棒 | 拿到底片链接后,**在输入框里填要精修的照片编号(逗号分隔,可填多个)**,提交即自动通知下一棒 |
| #guest 屏导语 | 怎么走完从**选片**到验收 | 怎么走完从**填编号选片**到验收 |
| #guest 引子 | 进度、选片、验收**全部自助** | 进度自查、**按编号提交精修需求**、一键验收全部自助 |
| 图 3 四棒句 | 摄影师传底片、**客人选片**、精修师交成品、客人验收 | 摄影师传底片、**客人填编号选片**、精修师交成品、客人验收 |

**全站「自助选片 / 点选 / 照片网格 / 勾选」字样残留 = 0。**

### 二、图 9 重画

原图画的是「3×3 照片网格 + 已选角标 + 提交选片按钮」——**与真实界面完全不符**。按用户实拍重画为:

```
浏览器窗框( …/delivery/**** 免登录链接 )
├── 交付进度条(一次性填充动画保留在 prefers-reduced-motion 块内)
├── 卡片一「底片已上传 [已完成]」→ 打码条 + 「网盘链接 + 提取码(此处为打码示意)」
├── 卡片二「填写选片需求 [进行中]」→ 标签「照片编号(逗号分隔)」
│                                   + 输入框「如:IMG_001, IMG_007, IMG_012」+ 计数「0 / 5000」
└── 按钮「取消」「确认提交」
```

★进度条不标步数也不标步名★:用户那张实拍是往下滚过的,画面里看不到顶部进度条;而源码里的主进度是 7 步、站上原先写的是「8 步」,两者对不上。在拿到能看清进度条的截图之前,不再声明步数——图注也写明了「进度条不代表实际步数」。

> **v17.1 已推翻本条**:这一轮拿到了能看清顶部进度的实机截图(客人页英文视图),实测为 **6 步**;图 9 已按实机重画,免责句已删。详见下方 v17.1 小节。

### 三、灰卡描述回改(撤销 v16.0 的错误改动)

v16.0 因为在 App 源码里 grep 不到弹层文案,把「灰卡可点击」改成了「只标注暂不可抢」。**这是错的**——用户实拍证实:点灰卡会打开这个订单的完整详情页。

| 位置 | v16.0(错) | v16.2(据实回改) |
|---|---|---|
| 十二格三 · 卡面 | 抢不了的单照样显示,报酬与城市照常可见。 | 抢不了的单照样显示,**点开还能看完整详情**。 |
| 十二格三 · 正文 | 照常显示为灰卡,**只标注「暂不可抢」**,报酬与城市不遮挡 | 照常显示为灰卡,报酬与城市不遮挡,并标出这单的门槛(如「需★≥5.0」);**点开还能看到这个订单的完整详情**——拍摄地、时间、拍摄时长、进店时间、人数、集合点、距离都在,只是底部按钮是「已被安排」而不是「抢」 |
| 图 8 提示 | 照常显示为灰卡…**只标注「暂不可抢」** | **灰卡可点开看完整详情**,报酬与城市照常可见,只是不能抢 |

也复核过:未经证实的「点击给改进路径」「提升评分可获得更多专属邀约」两句,自 v16.0 起已为 0 残留。

### 四、撤除旧图位

「三 · 可见性与可抢性分离」格内原有的那处旧图位(v16-A 标为「存疑 · 画面未核验」的早期机器截图)已撤除,对应图片文件一并删除。该格保留 v16.1 上线的 `app-grey-explain` 两图轮播——那两张是用户实拍,画面能直接印证上面那段回改文案。

### 统计

| 项 | v16.1 | v16.2 |
|---|---|---|
| 图片文件 | 28 | **27** |
| 占位处数 | 37 | **36** |
| 显示 / 空位 | 27 / 10 | **26 / 10** |
| 灯箱接线 / 灯箱大图 | 37 / 45 | **36 / 44** |
| 竖图位 | 14 | **13** |


---

## v17.0 图 0 按真实订单流转重画(总控台枢纽)+ 尾款门控整块删除

用户看图后指出:「网站的流程图乱糟糟的,根本不是正确的订单流通渠道和方向;**所有渠道流入之后必须要经过总控台**,然后进行分配和交付。」

本轮先**只读 `lvpai-console` 源码**把订单流转核对清楚,再据此重画图 0、彻查全站其余流程图。

### 一、核对出来的订单流转模型(每条都有出处)

> **一句话**:两个渠道的订单都只能由运营在总控台建单进系统;总控台在建单事务里绑定渠道规则、同时生成客人交付页链接,再由它向 App 派单、向客人分享交付页;两端各自回写到同一份订单数据,总控台据此结算。

| 环节 | 结论 | 出处 |
|---|---|---|
| 建单入口 | `POST /api/orders` 挂 `adminOnly` —— **没有面向平台或客人的公开建单接口** | `server/src/routes/orders.js:1838` |
| 全库写单点 | 全仓仅两处 `INSERT INTO orders`:标准建单 + 急单预建单,后者同样要 `admin/sales` | `routes/orders.js:817`、`routes/grab.js:704` + `grab.js:27-28` |
| 渠道字典 | 5 个渠道:`self / viator / gyg / ctrip / xiaohongshu`,各带 `default_payment_pattern` | `migrations/016a_create_channels.sql:26-31` |
| 渠道识别 | **是运营在建单表单里「选」的**(`input: 'select'`),不是系统自动识别 | `frontend/src/components/orders/ConversationalOrderBuilder.vue:319` |
| 渠道绑定规则 | 选定后自动派生:OTA 隐藏定金/尾款字段 + 按 `(channel_code, type)` 套 checkpoint 模板 | 同上 `:430,434`;`services/orders/applyCheckpointTemplate.js:17-37` |
| 派单发起 | 抢单发布 / 急单 / 定向邀请全在 `grab.js`,`requireAuth + requireRole(['admin','sales'])` —— 摄影师**不能**发单 | `routes/grab.js:27-28, 1037, 1019` |
| App 取单 | 摄影师 token(非 JWT)`GET /api/grab-portal/:token/offers` | `routes/grab_portal.js:260` |
| App 回写 | 抢单 `POST /api/grab-portal/:token` → 行锁 + `affectedRows` 守卫 + 唯一约束保证恰好一人;写 `order_items.photographer_id` + `INSERT schedules` 占档期 + 下架 | `grab_portal.js:280, 108`;档期填报 `staff_schedule_portal.js:316` |
| **交付页怎么产生** | **建单事务内自动生成 `share_token` 并回写 `orders.delivery_page_id`**;另有 admin JWT 手动补生成路由;`POST /share` 把 `created → shared` | `orders.js:866-882`(注释原文:「订单创建即可分享给客人」)、`delivery_pages.js:109/114/131`、`:251-284` |
| 交付页鉴权 | 公开 router 挂 `/api/delivery/:share_token`,**无 JWT**,只校验 token 字符串 + `expires_at` | `app.js:426`;`delivery_pages.js:288, 314-328` |
| 客人回写 | 选片是**填照片编号字符串数组**、验收/返修、客服事件;进度页 30 秒轮询 | `delivery_pages.js:720-724`(`customer_selection` 元素须为非空字符串)、`:804`;`frontend/src/views/Delivery.vue:1510` |
| 结算 | `requireAuth + admin + requireUnlock`,并**直接读 `grab_offers.offer_price_cny`** —— 公示价确实零转录进结算 | `routes/admin/order_settlement.js:34-35, 57-58` |

**与用户口述核对:完全成立**,且源码证据比口述更强——不是「流程上应该先过总控台」,而是**系统层面不存在绕过总控台的写入口**。

### 二、图 0 新旧结构对照 + 四处错误逐条修复

| 旧结构 | 新结构 |
|---|---|
| 三个端**平铺一行**(总控台 / App / 交付页 并列) | **四层枢纽**:两来源 → 汇聚 → 总控台(通栏) → App + 交付页 → 数据底座 |
| 来源两条线居中垂下,右侧那条落在交付页一侧 | 两条线各自锚在来源底边 → 汇成一条横杠 → **单箭头指进总控台顶边** |
| 交付页与总控台之间**零连线** | 双向两条线:总控台 →「分享交付链接」↓ / 交付页 →「选片 · 验收回写」↑ |
| `.sf-bridge` 里三个标签互相打架 | 四条线各自平行,标签贴各自线中段,**互不重叠** |

| 用户指出的错误 | 修复动作 | 实测证据(1280 视口,容器 974px) |
|---|---|---|
| ① 自营的下行箭头落在交付页一侧 | 两来源线汇聚成一条,只有**一个**箭头进总控台 | `cv-a` x=389.5 = 来源① 中心;`cv-b` x=890.5 = 来源② 中心;`cv-bar` 精确连接两者;`cv-mid` x=640 = 容器中心,底端 y **完全等于**总控台顶边 |
| ② 交付页与总控台无连线 | 新增 `ln-gd`(下行)+ `ln-gu`(上行) | `ln-gd` x=801.8、`ln-gu` x=977.2,顶端锚总控台底边、底端箭头进交付页顶边(y 精确相接) |
| ③ 三标签打架、线条交叉 | **删除「同一后台 · 两个界面」胶囊**(三端本就同一后台,该事实由「共享数据底座」表达);四线改平行竖线 | 全站宽度 360–1440px 扫描:**线交叉 0 处、标签重叠 0 处、标签越界 0 处** |
| ④「订单流入」浮在两线之间 | 改为「订单流入 · 建单时选定渠道」,贴在汇聚后那条线右侧 | 与线间距 **11px**(要求 ≥6px) |

其他硬指标(360 / 390 / 430 / 600 / 768 / 769 / 900 / 1100 / 1280 / 1440 十档实测):**标签与线最小间距 10px**,横向滚动 false。

窄屏(≤768px)纵向版:两来源堆叠 → 一条竖线汇入总控台 → 四条关系改为**带端名的文字行**(「总控台 → 派单下发」「App → 档期 · 接单回写」「总控台 → 分享交付链接」「交付页 → 选片 · 验收回写」),方向不可误读;汇聚标签距容器边 34px,不贴边。

### 三、全站其余流程图彻查

| 图 | 核对结论 | 修复动作 |
|---|---|---|
| hero 六站 | 六站顺序与真实生命周期一致;s1 文案「双渠道自动识别」**与源码不符** | 改为「双渠道 · 总控台建单」 |
| 图 3 七步旅程 | 七步起止端与角色标签全部正确(1 客人 → 2 建单管理端 → 3 派单管理端 → 4 接单 App → 5 拍摄 App → 6 交付客人页 → 7 结算管理端);第 1 步面板「进单时自动识别渠道」不符;第 2、6 步**漏了交付页链接的来源** | 第 1 步面板据实重写;第 2 步补「同一个事务里生成客人交付页链接」;第 6 步补「由总控台在建单时生成、再由运营分享」 |
| 图 4 三端业务流 | 六环节着色全部正确(客人下单/建单报价/结算=运营端,派单=运营+摄影师,拍摄=摄影师端,交付=客人交付页) | 仅面板一句「渠道自动识别」据实改写 |
| 图 6 派单对比 | 起止端与方向正确(客服/系统圈定候选 → 并行触达摄影师),与 `grab.js` 一致 | **已核对,无问题** |
| 图 7 抢单动线 | 六环节逐条对上源码:客服发布(admin/sales)→ 四道门槛圈人 → 多人竞抢 → 行锁保证一人成功 → 占档期+下架 → 公示价进结算 | **已核对,无问题** |
| 图 9 客人交付页 | 填编号选片 + 30 秒刷新均与源码一致(v16.2 已据实重写);唯缺链接来源 | #guest 屏引子补「链接由总控台在建单时生成、再由运营分享出去」 |
| 图 10 交付四棒 | 与公开 router 四个 PATCH 端点完全一致(摄影师传底片/客人选片/精修师交成品/客人验收,同一 share_token 页、无 JWT) | **已核对,无问题** |
| 图 11 对比表「接单」行 | 「渠道自动识别并绑定合规策略」不符 | 改为「渠道选定后自动绑定该渠道的支付与交付规则」 |

**全站「自动识别渠道」字样残留 = 0**(仅存一处「网盘链接与提取码自动识别」,已验证真实:`frontend/src/utils/workLink.ts:28` 确有提取码正则解析)。

### 四、尾款门控整块删除

用户要求不展示该功能。**注:该功能在生产代码里真实存在**(`delivery_pages.js:375-387, 466-476`,OTA 全额预付渠道永不门控),本轮只是从作品集里撤下,不是修正错误。

| 删除项 | 位置 |
|---|---|
| 「设计决策 · 尾款门控」整块(标题 + 两段正文 + 决策卡) | `#guest` 屏 |
| 图位 `lb-37`(`console-settlement-gate`)连同整个 `<figure>` | 同上 |
| 收益清单「尾款未付时成品链接自动隐藏…」一条 | `#guest` 收益清单 |
| 能力卡两处门控文案 | `#system` 订单公开页卡 |
| `.gatecard` 全部 CSS(含窄屏两条) | `style.css` |
| 两份 README 的门控行与计数 | `README.md` / `images/README.md` |
| `D3-尾款门控状态/` 文件夹 + `0-总说明.txt` 对照表 D3 行 | `~/Desktop/手动截图工作台/` |

**站点本体(`index.html` + `style.css`)grep「尾款」「门控」「settlement-gate」「gatecard」= 0 命中。**
(两份 README 里仅剩本节这样的**删除记录**——那是 5c 要求留档的内容,不是站点展示。)

### 五、计数变化

| 项 | v16.2 | v17.0 |
|---|---|---|
| 占位处数 | 36 | **35**(删 `lb-37`) |
| 显示 / 空位 | 26 / 10 | **26 / 9** |
| 灯箱接线 / 灯箱大图 | 36 / 44 | **35 / 42**(`lb-37` 是两图轮播,−2) |
| 图片文件数 | 27 | **27**(本轮不入库任何图片) |
| 竖图位 | 13 | **13**(`lb-37` 为横图) |
| 图 0 可点节点 | 5 | **6**(来源拆成 OTA / 自营两个独立节点) |
| `<details>` 总数 | 54 | **54**(图 0 +1,尾款门控块无 details) |

实测与预期**逐项一致**:1280 / 390 双视口 + `details` 全展开 → 图 35 / 显示 26 / 占位 9 / 破图 0 / 横滚 false / 锚点 31 条全有效 / `<script>` 0;`prefers-reduced-motion` 下三项指标与常态完全相同(零信息丢失)。

---

## v17.1 手动截图批量入库 + 放置决策落地

用户授权:**本批图片不打码,按现状入库**;但仍逐张视觉目检并留档敏感内容(见下)。

### 一、全量盘点

工作台 22 个夹子共 **23 个图片文件**,按 md5 去重后为 **21 张不同的图**
(`A2 / D1 / D2` 三个夹里是同一张图,逐字节相同)。全部为手机竖屏截图。

逐张打开看过之后,发现其中 **4 张早已入库**:C08 两张就是 `app-grab-hall-2/-3`,
C09 两张就是 `app-grey-explain/-2`(宽高比逐张比对一致)。

### 二、放置决策表(判定标准:内容必须与该位置正文讲的是同一件事;端别必须一致)

| 图 | 内容一句话 | 判定 | 去向 |
|---|---|---|---|
| A1 | 交付页选片区:编号输入框 + 0/5000 计数 + 确认提交 | **对号入座** | `guest-photo-select.jpg`(lb-17 / lb-36) |
| A2 = D1 = D2 | 与 A1 同一画面 | 暂不使用 | 同图不挂两处 |
| B1 | 锁屏上的急单推送:城市 / 报酬 / 时长 / 先到先得 | **对号入座** | `app-urgent-push.jpg`(lb-28) |
| C01-a | 发起抢单弹窗上半:硬门槛说明 + 报酬「即本单结算价」 | 暂不使用 | lb-12 已有同一弹窗的桌面图;竖图并入横图轮播会被 `object-fit` 压成细条 |
| C01-b | 同弹窗下半:定向邀请 / 定向排除 / 预览候选池 | 暂不使用 | 同上;「定向邀请」机制卡在折叠区内,无合适图位 |
| C02 | 阶梯发布梯队配置,「+ 再加一档(最多共 6 档)」 | 暂不使用 | lb-39 已有 `console-tier-setting` 同主题桌面图 |
| C03 | 进度节点 12 步 +「GYG 外网平台 · 和服旅拍」模板名 | **新增图位** | `console-checkpoint-template.jpg`(lb-41) |
| C04 | 结算看板:锚定/实时双口径 + 按渠道逐单明细 | 暂不使用 | lb-9 已有 `console-settlement-report` 同主题桌面图 |
| C05 | 预警阈值逐灾种配置(暴雨/大风/高低温/地震震度) | 暂不使用 | lb-14 已有 `console-weather-alert` 同主题桌面图 |
| C06 | 推送文案模板编辑 + 变量插入 + 发出效果预览 | 暂不使用 | lb-13 已是 `console-push-center` 两图轮播,同主题 |
| C07 | 抢单派发监控列表(进行中 1 · 超时 11) | 暂不使用 | 画面是监控列表,与 lb-22「派单方式**占比统计**」不是同一件事;且含多位客人姓名 |
| C08-a / C08-b | 抢单大厅可抢卡 / 抢单详情页 | 暂不使用 | **已在库** = `app-grab-hall-2 / -3` |
| C09-a / C09-b | 灰卡区(已派出 / 已被安排 / 需★≥5.0)/ 灰卡详情 | 暂不使用 | **已在库** = `app-grey-explain / -2` |
| C10-a | 客人收到的确认信 · 上半 | **新增图位** | `guest-confirm-mail.jpg`(lb-42 第 1 张) |
| C10-b | **交付页顶部**:Your Photoshoot Journey 六步 + 17% | **对号入座** | `guest-delivery-page.jpg`(lb-38) |
| C10-c | 确认信 · 下半:中文集合点指引 + Track Order Progress | **新增图位** | `guest-confirm-mail-2.jpg`(lb-42 第 2 张) |
| D4 | 添加档期 · AI 填写(自然语言 → 逐条确认再落库) | **对号入座** | `app-schedule-fill.jpg`(lb-24) |
| D5 | 档期月历,未来日期直接显示天气图标 | **对号入座** | `app-calendar-weather.jpg`(lb-30) |
| D6 | 历史抢单 142 条记录列表 | ~~暂不使用~~ → **v18-A 已入库** | ★上一轮判错★:当时拿它去比 lb-22「派单方式占比」,比错了对象。它对应的是 **lb-18 三方联动自动下架**——状态列「已抢到/进行中」正是「可见性与可抢性分离」的实证。客人姓名经用户授权按现状入库。 |

**入库 8 张,暂不使用 13 张(其中 4 张是重复入库、3 个文件是彼此的副本)。**
`lb-18 console-grab-offdown`、`lb-21 console-push-hitrate`、`lb-22 console-urgent-share`
三个占位本轮**仍空**——本批没有与它们讲的是同一件事的画面,宁可留白也不硬凑。

> **v18-A 后续**:`lb-18` 已用 D6 补齐;`lb-21` / `lb-22` 因生产系统里根本没有这两个界面,**整块删除**。

### 三、两个新增图位

| 图位 | 位置 | 为什么要新增 |
|---|---|---|
| **lb-41** `console-checkpoint-template` | 管理端「交付与精修」能力卡内 | v17.0 新写的「按**渠道 × 订单类型**套用交付节点模板」此前**没有任何配图**;lb-15 那张是同一个抽屉的**交付内容** tab,不是同一画面 |
| **lb-42** `guest-confirm-mail` + `-2`(两图轮播) | 管理端「邮件自动化」能力卡内 | 站上「8 类信件模板,**中英双语**」此前只有总控台侧的模板管理页,没有客人**实际收到**的信;这封信底部的「Track Order Progress」还正好实证了 v17.0 图 0 那条「分享交付链接」 |

### 四、图 9 进度条据实重画(任务三)

拿到了能看清顶部进度的实机截图(C10-b,客人页英文视图),实测:

| 项 | 实测 |
|---|---|
| 步数 | **6 步**(角标 17% ÷ 已完成 1 步 = 1/6 ≈ 16.7%,与步数自洽) |
| 步名(画面原文) | ① Order Created ② Meeting Photographer ③ Shooting Completed ④ Awaiting Photographer Upload ⑤ Awaiting Customer Selection ⑥ Awaiting Retouching |
| 形态 | **竖排步骤列表**,不是横向进度条;当前步高亮,已完成打勾 |

改前 → 改后:

| 位置 | 改前 | 改后 |
|---|---|---|
| 图 9 进度区 | 横向进度条 + **5 个圆点**(无步名) | 六步竖列 + 步名原文 + 百分比角标(图中状态为「已完成 4 步」→ 67%) |
| 图 9 图注 | 「界面元素为示意,**进度条不代表实际步数**」 | 删除该免责句,改为「顶部**六步进度按实机重画**——步名为客人页英文视图的原文(页面支持中英切换),百分比即「已完成步数 ÷ 6」…下方卡片为示意」 |
| README 页面结构 | 「配图9,**8 步**进度」 | 「配图9,**6 步**进度 · 按实机重画」 |
| README v16.2 记录 | 「在拿到能看清进度条的截图之前,不再声明步数」 | 原文保留(它当时是对的),下方加一行 **v17.1 已推翻本条** |

CSS 侧:`.odp-ptrack / .odp-pfill / .odp-pdots` 与 `v13-odpfill` 动画一并删除,换成
`.odp-phead / .odp-pct / .odp-steps`;动效删除不影响信息量。

### 五、敏感内容留档(用户已知情并授权,仅留档,不作拦截)

**已入库的 8 张**:

| 文件 | 类别 | 大致位置 |
|---|---|---|
| `guest-photo-select` | 网盘链接(完整可读)、摄影师姓名 | 链接在页面中部「已完成」卡内;姓名在顶部订单信息行 |
| `guest-confirm-mail` | 客人姓名 | 信件正文称呼行 |
| `guest-confirm-mail-2` | 集合点街道地址(公共地标周边,非住址) | 中部 Meeting Point 区块 |
| `app-urgent-push` | **可辨识人脸 ×2** | 锁屏壁纸主体(通知条上下方) |
| `console-checkpoint-template` | 订单号 | 顶部标题行 |
| `guest-delivery-page` / `app-schedule-fill` / `app-calendar-weather` | 未见姓名 / 人脸 / 链接 | — |

**未入库但已目检的**:C07 弹窗表格「客户」列约 7 位客人姓名;D6 弹窗表格「订单」列约 10 位客人姓名;
C04 顶部 KPI 卡含流水 / 成本 / 毛利金额。这三张的敏感度也是本轮判定「暂不使用」的加权因素之一。

### 六、计数变化

| 项 | v17.0 | v17.1 | 算式 |
|---|---|---|---|
| 图片文件数 | 27 | **35** | 27 + 8 |
| 占位处数 | 35 | **37** | 35 + 2(lb-41、lb-42) |
| 显示 / 空位 | 26 / 9 | **34 / 3** | 26 + 6(填了 5 个文件,其中 `guest-photo-select` 占 2 个位)+ 2(新图位) |
| 灯箱接线 / 灯箱大图 | 35 / 42 | **37 / 45** | 42 + 1(lb-41 单图)+ 2(lb-42 两图) |
| inline shotimg | 42 | **45** | 与灯箱大图一一对应 |
| 竖图位 | 13 | **15** | 13 + 2 |

实测与预期**逐项一致**:1280 / 390 双视口 + `details` 全展开 →
图 37 / 显示 34 / 占位 3 / 破图 0 / 横滚 false / 锚点 31 条全有效 / `<script>` 0;
竖图专项 15 张**全部 ≤78vw 且渲染比与原始比一致(零裁切)**;
轮播专项 lb-42 两张全部载入、圆点显示、灯箱大图数匹配;孤儿文件 0;
`prefers-reduced-motion` 下三项指标与常态完全相同。

---

# v18-A · 全站图片视觉总对位 + 空位处置 + logo 字标(2026-08-22)

基线 `6761d56`(tag `v18.0-pre-baseline`,本地与远端同哈希)。回滚:
`git reset --hard v18.0-pre-baseline && git push -f origin main`

## 一、这一轮真正做的事

前几轮判断配图靠的是**文件名**。这一轮改成:**把每一张已显示的图逐张打开看画面**,
写下"画面里到底有什么",再和它所在位置的标题对照。只认画面,不认文件名。
结果是——文件名骗了我们不止一次:

- `console-orders-list`(总控台订单列表)**整屏没有一行订单**,它和 `console-alert-board` 几乎是同一屏;
- `app-income-bill`(个人收入账单)与 `app-magic-link`(专属链接首屏)**是同一个「📍位置设置」弹层**,
  只有背后的标签页不同——两处都跟自己该讲的事毫无关系;
- `console-grab-stats`(抢单成交率统计页)画面其实是**抢单派发监控**,而且显示"超时 11";
- `console-weather-alert`(预警详情弹层)画面其实是**突发天气预警配置区**,没有弹层。

完整逐图表格见 `images/README.md` 的〈v18-A · 全站图片视觉总对位〉。

## 二、处置分三种,不做近似顶替

| 处置 | 适用 | 本轮命中 |
|---|---|---|
| **改标** | 画面是真的、也确实支持所在段落的论点,只是标题名不符实 | lb-1/19、lb-2/20、lb-7、lb-14、lb-32 共 6 处 |
| **撤图 + 建夹** | 画面与所在位置讲的完全不是一回事,且库内与工作台都没有对的图 | lb-6 → E1、lb-31 → E2、lb-34 → E3 |
| **整块删除** | 生产系统里压根没有这个界面,挂空框只是难看 | lb-21、lb-22 |

**为什么不拿相近的图顶上**:`console-order-edit` 确实露出了订单表头和 1 行数据,
看着像能顶 lb-6。但它只有 1 行且底部被截断,而 lb-6 那一段正文讲的是"建单从三个人忙一天
变成一个人四分钟"——底下配一张看不清订单的图,等于换一种方式糊弄。宁可回落成占位框,
把要求写进 E1 的 README 里,让下一张图一次到位。

撤下的三张原图留档在 `~/Desktop/手动截图工作台/_错配留档/`,没有丢。

## 三、逐项修复

| # | 用户反馈 | 处置 |
|---|---|---|
| ① | logo 仍是空的 | 两处 `LOGO` 虚线空框 → **纯 CSS 字标**:导航条是琥珀「榴」字印章,首屏是「榴档期」+ 小字「LIULIAN · 旅拍调度」。同时建 **E0-品牌LOGO** 夹,拿到真 logo 图再换图片版 |
| ② | 图 2 双端合成图左半没截出订单 | 属实(左半用的正是那张没有订单行的图)。**按令本轮不动此图**,已在两份 README 记下待办:E1 到位后重新合成 |
| ③ | lb-18 用户已给图(D6),上轮比错对象 | **已入库** `console-grab-offdown.jpg`(825×1688,174 KB),转竖图容器,图注据实重写 |
| ④ | lb-21/lb-22 空框观感差 | 两个 `figure` **整块删除**;KPI 数字、CSS 对比条、统计口径全部保留不动 |
| ⑤ | 十二格「八·收入自己查」「十一·免注册长效登录」配图错 | 逐图看画面后**确认属实**(两处都是「位置设置」弹层)→ 撤图 + 建 E2 / E3 |
| ⑥ | 建单对比图与管理端 5 块区域配图错 | 建单对比图(lb-6)**确认错配**→ 撤图 + 建 E1;管理端 5 块**逐块复核后全部匹配**,仅「订单管理」一处标题名不符实,已改标 |
| ⑦ | 「图已显示但占位文案仍可见」的残留 | 双视口实测 **0 处**。机制本身正确;用户看到的就是当时那 3 个真没图的位。本轮后这三处占位文案全部消失 |
| ⑧ | #system 摄影师端 App 卡只有链接没有简介 | 链接前补**两行**简介,从专章开头压缩改写,**零新数字** |
| ⑨ | App 专章「三块主界面」CSS 简笔画换真图 | 简笔画(HTML + `.dg-three`/`.tp*` CSS)**整块删除**,换成真机截图轮播 `lb-43` |

### ⑨ 的一处如实说明

原令指定轮播用 `app-calendar-weather` / `app-grab-hall` / `app-income-bill` 三张,
理由是"三张均在库"。但本轮 1a 逐图核查(该核查被指定为**最终判**)证明
**`app-income-bill` 画面并不是收入账单**,正是反馈⑤要撤的那张。
因此轮播先上**两张已核实的真图**,第三格等 E2 到位再并入;图注里写明了这一点,没有拿错图凑数。

## 四、验证(预期先算,再实测)

预期算式:图片 35 − 3(撤图)+ 1(D6 入库)= **33**;
图位 37 − 2(删框)+ 1(新增 lb-43)= **36**;
有图 34 − 3 + 1 + 1 = **33**;占位 3 − 1 − 2 + 3 = **3**;33 + 3 = 36 ✓

实测(headless Chrome,`<details>` 全展开):

| 指标 | 1280 视口 | 真 390 视口 | 预期 |
|---|---|---|---|
| 图位 | 36 | 36 | 36 ✓ |
| 有图 / 占位 | 33 / 3 | 33 / 3 | 33 / 3 ✓ |
| 图与占位同时可见 | 0 | 0 | 0 ✓ |
| 破图(真实渲染) | 0 | 0 | 0 ✓ |
| 横向滚动 | false | false | false ✓ |
| 失效锚点 | 0 | 0 | 0 ✓ |
| `<script>` 标签 | 0 | 0 | 0 ✓ |

`prefers-reduced-motion: reduce` 下:图位/有图/占位/页高/破图**与常态逐项相同**,零信息丢失。
(390 视口说明:macOS 版 Chrome 窗口最小宽约 500px,`--window-size=390` 会被夹到 500;
本轮改用 iframe 承载,实测 `clientWidth = 390`,是真 390 而非夹取值。)

## 五、下一轮待办

1. **E1 到位后重新合成图 2**(`console-app-mirror` 左半换成真正的订单列表)
2. E2 到位后并入 `lb-43` 轮播第三格
3. `lb-32`「作品提交有回音」现挂的是**提交侧**画面(优秀作品提交弹层,记录为空),
   没有审核结果——是否也算错配,请裁决;若算,再建一个 E 类夹
4. `guest-photo-select-2`、`guest-delivery-page-2` 两处第 2 张预留位仍无文件
   (不显示、不破图,仅各一次落空请求;放图即生效)

---

# v18-B · 全站化繁为简重构:七屏双主线 + 证据条 + 订单目录(2026-08-22)

基线 `a9584aa`(tag `v18a-baseline`,本地与远端同哈希)。回滚:
`git reset --hard v18a-baseline && git push -f origin main`

★本轮 `images/` 目录零改动★——不入库、不删除任何图片文件,36 个图位的 `lb` 编号一个没变,
只是物理位置从 11 屏迁进了 7 屏。

## 一、为什么重构

原站 11 屏、正文冗长,读者要读很久才知道「这是什么、是不是真的」。
v18-B 按「总控台 / App 双主线」重排为 **7 屏**,并做三个升级:
**①首屏证据条**(真实性优先于能力)**②图 3 七步升级为全站可点目录**
**③每屏首句从「这一屏讲:…」元描述改成结论句**。

## 二、七屏结构与各屏首句

| 屏 | 锚点 | 首句(结论句) |
|---|---|---|
| 1 · 首屏 | `#top` | (一句话定义)一套真实在跑的旅拍业务系统:订单、派单、拍摄、交付、结算全链路自动流转,由三端协同。 |
| 2 · 全景与目录 | `#map` | 所有订单先进运营总控台,再由它派给摄影师、分享给客人——系统里没有第二个建单入口。 |
| 3 · 运营总控台 | `#console` | 工作室的活全在这一个后台里:建单、派单、交付、结算,四件事一条线走完。 |
| 4 · 榴档期 App | `#app` | 摄影师愿意用,因为报酬前置、先到先得——上线两个月承接三分之一派单量。 |
| 5 · 模式优势 | `#why` | 差别不在某一步更快,而在整条链路上「必须有人在场」的位置变少了。 |
| 6 · 数字与口径 | `#data` | 下面每个数字都写明了算法与统计范围,尚未核验的部分也一并标了出来。 |
| 7 · 如何交付 | `#how` | 靠的是一套流程:让 AI 这种输出不稳定的执行者,也能稳定产出可靠结果。 |

全站 `grep 这一屏讲` = **0**。

## 三、三个升级怎么落地的

**① 首屏证据条 `.proofbar`** —— 五项全部是现站既有事实,零新数字:
App Store 在售(真实链接徽章)· 481 笔真实订单 · 9 个城市与地区 · 一个人运营 · 2026.05–2026.08 持续在跑。

**② 图 3 → 全站目录** —— 七步横条保留,每步整体变成页内锚点链接(`<a class="jstep">`),
原来的 `details` 深论证压缩成一句话进 `title`。跳转映射:

| 步 | 去向 | 步 | 去向 |
|---|---|---|---|
| 1 客人下单 | `#c-build` | 5 拍摄 | `#a-push` |
| 2 建单 | `#c-build` | 6 交付 | `#c-deliver` |
| 3 派单 | `#c-dispatch` | 7 结算 | `#c-settle` |
| 4 摄影师接单 | `#a-hall` | | |

**③ 结论式首句** —— 见上表。原 `.screenlead` 元描述句全部替换为 `.lead` 结论句。

## 四、影片位(v18-C 已交付)

首屏两个 16:9 容器 `.film-slot`(**客人篇** / **客服篇**)已在 v18-C 轮替换为
**纯 CSS 模拟操作影片**,容器栅格、16:9、窄屏纵向堆叠均沿用未动。
详见文末〈v18-C · 首屏双视角影片〉。

## 五、删除项清单与理由(零静默丢失)

| 删除项 | 理由 | 内容去向 |
|---|---|---|
| **图 4 · 三端一条业务流** | 六个面板讲的事与图 0(枢纽全景)、图 3(七步旅程)完全重合,三图同题 | 逐句比对确认无独有信息;保留信息量更大的图 0 与图 3 |
| **图 10 · 交付四棒交接对比** | 与图 11(人工介入次数对比)二选一;图 11 覆盖接单→结算全链路,图 10 只覆盖交付 | 图 10 的论点(客服中转不产生价值,只产生延迟)已由对比表「交付」行与屏 3 交付块正文承载 |
| **对比表 4 行**(建单 / 拍前 / 拍摄 / 复盘) | 8 行砍到 4 行,保留反差最大的接单 / 派单 / 交付 / 结算 | 建单→屏 3 建单块与图 5;拍前拍摄→屏 4 推送与预警;复盘→屏 6 数字与口径 |
| **dispatch 版 74.7% / 2.0 分钟 KPI 卡** | 与首屏 hero 版同口径同公式,纯重复 | 卡内引用图 6 时间轴的两段评述(8/23/41 分钟)已迁入屏 3 派单块折叠「为什么这是结构变化」;**数字本体在图 6 内,图 6 保留** |
| **`.screenlead` 元描述句 ×10** | 升级三要求改结论句 | 全部改写为 `.lead` |

**没有删除任何数字。** 屏 3/屏 4 的十二格与十二项全部并入主线块,深论证进 `details`。

## 六、内容归属速查

- **管理端 11 块**:订单管理→`#c-build`;交付与精修 + 订单公开页→`#c-deliver`;结算体系→`#c-settle`;
  派单调度→`#c-dispatch`;AI 能力→`#c-build` 折叠;邮件 / 监控 / 评分 / 推送 / 预警→`#console` 能力清单(一行式 + 缩略图直通灯箱 + 完整折叠)
- **App 12 项**:一→`#a-slot`;二三四→`#a-hall`;五六七→`#a-push`;八九十→`#a-income`;十一十二→章末 `.append`
- **客人交付页**:整章并入屏 3 `#c-deliver`(图 9 + 四条收益点 + lb-38/17)
- **自动化与 AI**:AI 四块→`#c-build` 折叠;系统自己做的九件事→`#why` 折叠;图 11→`#why`

## 七、验证

预期:屏 7 · 图位 36(lb 编号不变)· 有图 33 / 占位 3 · 灯箱 36 套 · `<script>` 0 · 导航 7 项 · 内部锚点 25 条。

| 指标 | 1280 | 真 390 | 预期 |
|---|---|---|---|
| 屏数 | 7 | 7 | 7 ✓ |
| 图位 | 36 | 36 | 36 ✓ |
| 有图 / 占位 | 33 / 3 | 33 / 3 | 33 / 3 ✓ |
| 图+占位同现 | 0 | 0 | 0 ✓ |
| 破图 | 0 | 0 | 0 ✓ |
| 横滚 | false | false | false ✓ |
| 失效锚点 | 0 | 0 | 0 ✓ |
| 内部锚点总数 | 25 | 25 | 25 ✓ |
| `<script>` | 0 | 0 | 0 ✓ |
| 导航项 | 7 | 7 | 7 ✓ |

**内容完整性抽查 20/20 在场**;管理端 11 块、App 12 项、证据条 5 项全部在场;
`prefers-reduced-motion` 下图位 / 有图 / 占位 / 页高 / 破图与常态**逐项相同**,零信息丢失。

首屏六站动画高度:桌面 **118px**(要求 ≤120px ✓);
窄屏 390 为 211px —— 6 站 × (图标 + 站名 + 说明) 在 390px 宽度内无法压进 120px 而不删字,
**选择保留文字、不达标并如实标注**(已改 3×2 网格并收紧字号,由 232px 降到 211px)。

文字校对:错别字 0、连续空格 0、标点风格一致(全站半角逗号 336 / 全角逗号 0,半角冒号 164 / 全角冒号 0,全角句号 237);
中英空格仅 4 处命中,全部是 `08月14日` 这类日期写法,非缺陷。

---

# v18-C · 首屏双视角影片:客人篇 + 客服篇(纯 CSS 模拟操作)(2026-08-22)

首屏那两个 16:9 占位卡换成了**两部约 14 秒无缝循环的纯 CSS「模拟操作影片」**。
同屏对比**传统模式(灰调手绘示意)vs 本系统(彩色真实截图)**,假光标模拟点击。

**没有引入任何新文件、新图片、新脚本。** 界面帧全部是 `images/` 里**已有的真实截图**,
用 `overflow:hidden` 容器 + `transform: translate() scale()` 在浏览器里裁切放大,
所以 `images/film/` **一个派生帧都没有生成**(总体积 0 KB / 0 个文件)。

## 一、为什么是「示意 + 真截图」的混排

传统模式**没有真实界面可截**——它就是微信里的一串对话。所以左半屏是 CSS 手绘的聊天气泡
与计时器(灰调、无品牌、无头像),**明确是示意**;右半屏一律是**站内已在用的真实截图**。
这样对比是诚实的:不拿假截图冒充传统系统,也不拿示意图冒充本系统。

## 二、分镜表

### 客人篇(客人视角)· 总长 14.0s

| 秒段 | 传统侧(灰调示意) | 系统侧(真实截图) | 光标动作 | 字幕 |
|---|---|---|---|---|
| 0.0–2.9s | 气泡①「拍好了吗?」(0.7s 出现) | `guest-confirm-mail-2` 裁到 **Track Order Progress** 按钮区 | 0.7s 淡入 → 1.7s 落到按钮 → **2.8s 点击波纹** | 确认信底部就是一条进度链接 · 不用注册、不用装 App |
| 2.9–5.8s | 气泡②「…」(3.4s 出现) | `guest-delivery-page` 裁到**六步进度整卡** | 3.4s→5.6s 顺着六步**自上而下扫读** | 点开就是六步进度 · 页面每 30 秒自动刷新 |
| 5.8–8.7s | 气泡③「在吗?什么时候能看到片子」(6.7s) | `guest-photo-select` 裁到**编号输入框特写** | **7.4s 点进输入框** → 6.7–8.1s **打字「IMG_001, IMG_007」** | 拿到底片后,在输入框里填要精修的照片编号 |
| 8.7–12.1s | 气泡④「稍等,我问一下摄影师」(9.8s) | 同图右移到 **Confirm Submit** 按钮区 | 9.9s 移到按钮 → **10.8s 点击** → 10.6–11.9s **绿色对勾** | 提交即自动通知下一棒 · 验收一键完成 |
| 12.0–13.4s | — | **定格总结卡** | 淡出 | 「客人全程免登录 / 一条链接走完选片到验收」 |
| 13.4–14.0s | 卡片淡出,各层归位,**无缝接回 0.0s** | | | |

左下角「等客服回复」虚线条**全程呼吸式明暗**(2.4s 一次),表达「一直在等」。

### 客服篇(运营视角)· 总长 14.0s

| 秒段 | 传统侧(灰调示意) | 系统侧(真实截图) | 光标动作 | 字幕 |
|---|---|---|---|---|
| 0.0–3.2s | 计时 **0 分钟** + 行①「摄影师 A / 没空」 | `console-ai-parse` 裁到**新建订单弹窗 · AI 粘贴框** | 0.8–2.2s **粘贴文本逐字出现** → **2.2s 点「AI 识别填写」** | 整段订单文本粘贴进来,AI 自动解析成表单字段 |
| 3.2–5.8s | **2.8s 跳 8 分钟** + 行②「摄影师 B / 未回复」 | 同图下移到**基本信息字段区** | 光标移开 | 3.9–5.7s **琥珀色高亮自左向右扫过字段** | 字段自动填好 · 建单环节全程 2–4 分钟 |
| 5.8–9.0s | **6.2s 跳 23 分钟** + 行③「摄影师 C / 档期冲突」 | `console-grab-modal` 裁到**候选条件 · 四道门槛 · 评分档位 · 阶梯发布** | **7.8s 点「阶梯发布」** → 7.8–9.0s **广播脉冲自弹窗中心扩散** → 8.5s **「推送已发」标签** | 四道门槛自动圈人 · 评分梯队分层放号 |
| 9.0–12.3s | **9.0s 跳 41 分钟** + 行④「摄影师 D / 没空」 | 总控台淡出,`app-grab-hall-2` **手机竖屏**淡入,裁到**抢单大厅蓝卡** | 光标移到卡片 → 10.6–11.1s **「已被抢」红章盖下** | 一次推送先到先得 · 接单时长中位数 2.0 分钟 |
| 12.2–13.4s | — | **定格总结卡** | 淡出 | 「发布一次 · 系统圈人 / 先到先得,并发保证恰好一人成功」 |
| 13.4–14.0s | 卡片淡出,各层归位,**无缝接回 0.0s** | | | |

计时器 **0 → 8 → 23 → 41 分钟**与四行摄影师**逐拍同步**,
数字与人名结论**与屏 3 的图 6 串行时间轴逐行一致**(A 没空 / B 未回复 / C 档期冲突 / D 没空),**没有新造一个数字**。

## 三、素材帧清单与来源映射(0 个派生文件)

八个动画帧全部由 `images/` 原图 CSS 裁切得到,**没有写入 `images/film/`**:

| 帧 | 源文件(images/) | 裁切(占原图比例) | 对准的界面元素 |
|---|---|---|---|
| 客人① | `guest-confirm-mail-2.jpg` | 宽 8.3–91.7% / 高 17.0–73.7% | Track Order Progress 按钮(高 57.6–64.0%) |
| 客人② | `guest-delivery-page.jpg` | 宽 4.3–96.9% / 高 22.0–85.9% | 六步进度整卡(高 26.1–81.5%) |
| 客人③ | `guest-photo-select.jpg` | 宽 7.2–59.8% / 高 59.0–94.5% | 编号输入框 + Cancel / Confirm Submit |
| 客人④ | `guest-photo-select.jpg` | 宽 43.7–96.3% / 高 60.0–95.5% | Confirm Submit 按钮(宽 62–92% / 高 85.0–88.5%) |
| 客服① | `console-ai-parse.jpg` | 宽 25.4–75.4% / 高 11.3–59.8% | 新建订单弹窗 + AI 智能识别粘贴框 |
| 客服② | `console-ai-parse.jpg` | 宽 25.4–75.4% / 高 36.3–84.8% | 基本信息字段区 |
| 客服③ | `console-grab-modal.jpg` | 宽 25.4–75.4% / 高 25.3–66.9% | 候选条件 / 硬门槛 / 报酬 / 评分档位 / 阶梯发布 |
| 客服④ | `app-grab-hall-2.jpg` | 宽 3.6–54.4% / 高 38.4–78.3% | 抢单大厅蓝卡(东京 · 浅草寺) |

**手机壳、光标、波纹、对勾、红章、进度条一律纯 CSS 绘制**,零下载素材、零外链、零 SVG 文件。
`.fm-paste` 里的粘贴文本**不含任何数字与客人信息**,措辞与截图内的示例同源(和服写真 / 英语客人),
并按 1280 / 390 两个视口的粘贴框内宽(151px / 99px)实测定长,**不会被裁断**。

## 四、暂停开关

每部右上角一个 **44 × 44px** 圆形 ⏸ / ▶ 按钮:纯 CSS `checkbox + label`,
`:checked` 时对该部 `.fm-stage` 内**全部动画**(含 `::after` 伪元素)置 `animation-play-state: paused`。
无头实测:客人篇 16/16 层、客服篇 21/21 层由 `running` 全部转 `paused`,
`getComputedStyle` 读回 `paused`,**两个视口热区均为 44×44px**。

## 五、reduced-motion 降级(信息零丢失)

`prefers-reduced-motion: reduce` 下隐藏动画舞台与暂停键,改显**该篇 4 格静态分镜**
(关键帧 + 一句话)+ 一行两侧对比结论。强制渲染实测:两部 `display` 均为 `none`,
`document.getAnimations()` 返回 **0**,静态块各 **4 格**齐全,横滚 false。

静态分镜与影片**共用同一套 transform 裁切算式**(按 6:5 框重算),
所以「六步进度」在静态格里也是**六行全部入框**,「粘贴」与「字段填好」两格取景明显不同。

## 六、性能与铁律自检

| 项 | 客人篇 | 客服篇 | 要求 |
|---|---|---|---|
| 主时间轴时长 | 14.0s | 14.0s | 12–16s ✓ |
| 动画层数 | 16 | 21 | ≤25 ✓ |
| 动画属性 | `transform` / `opacity` / `clip-path` / `width`(打字) | 同左 | 无 layout 动画(打字 `width` 属小元素豁免)✓ |
| 外部资源 / 外链 / `<script>` | 0 / 0 / 0 | 0 / 0 / 0 | 全 0 ✓ |
| 新造数字 | 0 | 0 | 0 ✓ |

## 七、无头验证(1280 + 真 390 双视口)

用 `document.getAnimations()` 把两部影片的 `currentTime` 分别跳到 **t=2s / 6s / 11s**,
两视口共截 **12 帧**逐帧目检(另 4 帧 reduced-motion),结论:
**光标位置、字幕内容、裁切取景三项逐帧对得上,无一帧错位。**

与 v18-B 基线(`dec8e8d`)逐项比对,**全部持平**:

| 指标 | v18-B 基线 | v18-C | 结论 |
|---|---|---|---|
| 图位 | 36(有图 33 / 空位 3) | 36(有图 33 / 空位 3) | 不变 ✓ |
| `<script>` | 0 | 0 | 不变 ✓ |
| 内部锚点 | 25 | 25 | 不变 ✓ |
| 破图 | 0 | 0 | 不变 ✓ |
| 横滚(1280 / 390) | false / false | false / false | 不变 ✓ |
| 页高 1280 | 31741px | 31741px | 完全一致 ✓ |
| `figure.fig` | 10 | 10 | 不变 ✓ |
| `[id]` 总数 | 68 | 70 | +2(两个暂停开关 checkbox)|
| `<img>` 总数 | 90 | 106 | +16(8 动画帧 + 8 静态分镜帧,均复用库内原图)|

## 八、本轮修掉的实现缺陷(审计组回执)

1. **静态分镜作用域错误**:`.fm-b .fstN` 选择器命中不了 `.fm-static`(`.fm-b` 在舞台上,不是静态块的祖先),
   导致**客服篇四格全部套用客人篇取景**,加上 `object-fit: cover` 在近似同比例下几乎不起作用,
   ①②两格看起来一模一样。改为 `[data-film="b"]` 作用域 + transform 裁切。
2. **粘贴文本虚构客人信息**(含时间与人数),违反「不新造数字/案例细节」铁律,已改为无数字措辞。
3. **交叉溶解过长**(3% ≈ 0.42s),两张密集截图叠影发灰;收紧到 1.5% ≈ 0.21s。
4. **390 字幕折行溢出**字幕条并压住左侧条目;字幕条加高到 44px,窄屏隐藏「仍无着落」条让位。
5. **「已被抢」红章在 390 折成两行**;加 `white-space: nowrap`。
6. **390 下「本系统 · …」标签与暂停键重叠 13.9px**(实测);收字号与位置,复测重叠 false 且按钮完整落在舞台内。
7. **客服篇只有 3 位摄影师却计到 41 分钟**,与图 6 对不上;补第 4 位并让四行与 0/8/23/41 逐拍同步。
8. **光标在循环起点 opacity 从 0 跳到 .95**;改为淡入淡出,循环接缝不可见。
9. 清掉 v18-B 遗留的占位卡死代码(`.fs-16by9` / `.fs-play` / `.fs-badge`,`index.html` 已 0 引用)。

---

# v18-D · 图号与结构引用一致性巡检 + 工作台待办对齐(2026-08-22)

v18-B 删掉了图 4 与图 10、解散了十二格与十二项、把三个专章并进双主线。
这一轮专查**那场大手术留下的残留**,并把桌面工作台的待办对齐到真实缺口。

## 一、图号重排:既有空洞,又有乱序

巡检发现全站图号是 `0,1,2,3,5,6,7,8,9,11` —— **空洞两处(4、10)**,
而且用无头浏览器读 `figcaption` 的真实 `top` 坐标排序后发现,**编号顺序与浏览顺序不一致**:
读者往下滚,会先遇到「图 2」再遇到「图 1」,先遇到「图 9」再遇到「图 8」。

已全站重排为 **图 0–图 9 连续,且编号顺序 = 浏览顺序**:

| 旧号 | 新号 | 图名 | 所在位置 |
|---|---|---|---|
| 图 0 | **图 0** | 系统全景 | 屏 2 全景与目录 |
| 图 2 | **图 1** | 双端界面并排 | 屏 2 全景与目录 |
| 图 1 | **图 2** | 四类使用者各自拿到什么 | 屏 2 全景与目录 |
| 图 3 | **图 3** | 全站目录 · 一条订单的完整旅程 | 屏 2 全景与目录 |
| 图 5 | **图 4** | 建单流程对比 | 屏 3 运营总控台 · 建单 |
| 图 6 | **图 5** | 派单模式对比 | 屏 3 运营总控台 · 派单 |
| 图 7 | **图 6** | 抢单流转动线 | 屏 3 运营总控台 · 派单 |
| 图 9 | **图 7** | 客人交付页界面示意 | 屏 3 运营总控台 · 交付 |
| 图 8 | **图 8** | 抢单大厅界面示意 | 屏 4 榴档期 App · 抢单大厅 |
| 图 11 | **图 9** | 人工介入次数对比 | 屏 5 模式优势 |
| ~~图 4~~ | —— | 三端一条业务流 | v18-B 已删 |
| ~~图 10~~ | —— | 交付四棒交接对比 | v18-B 已删 |

**交叉引用同步范围**:`index.html` 正文经全文扫描**没有任何「见图 N」式交叉引用**
(10 处「图 N」全部是 figcaption 自身标签),因此站内只需改这 10 处;
`README.md` 现状章节(动效表、页面结构、配图清单)与桌面工作台 `0-总说明.txt`
里的图号引用已逐处同步。

> **历史版本记录章节(v15.0 – v18-C)里的图号一律保持原样不动** ——
> 那些是各版本当时的编号,改了就等于篡改历史。看那些章节时请对照上表换算。

## 二、1b 死结构引用逐词核查表

| 词 | index.html 命中 | 判定 |
|---|---|---|
| 十二格 / 十二项 / 十二块 / 十二个 | 0 | ✅ 已核对,无问题(v18-B 解散后站内已零残留)|
| 点开任意一格 / 任意一格 | 0 | ✅ 已核对,无问题 |
| 第 N 屏 | 0 | ✅ 已核对,无问题(站内不写屏号,只用锚点名)|
| 这一块单独有一个 | 0 | ✅ 已核对,无问题 |
| 十一屏 / 十二屏 | 0 | ✅ 已核对,无问题 |
| **专章** | **2** | ❌ 已改写:「本端有专章 →」→「详细 →」(客人交付页在 v18-B 后是屏 3 的一个**块**,不再是专章);「看 App 专章 →」→「看 App 主线 →」(与 kicker「主线 · 二」一致)|
| 上表 / 下表 | 下表 ×2 | ✅ **保留**:两处都紧跟着真表格(图 1 后的字段映射表、屏 5 的全链路对比表),指向真实存在的东西 |
| 九件事 | 1 | ✅ **保留**:实测折叠区内 `.au` 条目 **9 个**,数量描述属实 |
| 三组 | 1 | ✅ **保留**:指「人机协作三方分工」方法论,与站点结构无关 |

## 三、1c 锚点与导航语义

**已核对,无问题**:7 个导航项(概览 / 全景与目录 / 运营总控台 / 榴档期 App /
模式优势 / 数字与口径 / 如何交付)全部命中真实 `section`;内部锚点 **25 条、失效 0**;
六个 kicker(全景 / 主线 · 一 / 主线 · 二 / 模式优势 / 数字与口径 / 如何交付)与七屏结构相符。

## 四、1d README 与站点互证(实测比对后更新)

| README 旧陈述 | 站点实测 | 处置 |
|---|---|---|
| 页面结构(**十一屏**) | 7 屏 | 改写为七屏结构表 |
| 顶部导航「客人端 / 系统构成 / 管理端 / 派单调度 / …」 | 概览 / 全景与目录 / 运营总控台 / 榴档期 App / 模式优势 / 数字与口径 / 如何交付 | 全部改正 |
| 十二个能力板块分三组 · App 十二项差异化网格 | 已解散为**双主线各四块** | 改写 |
| 54 处 `<details>` | **33 处** | 改正 |
| 1 组 radio 选项卡(图 4 六环节) | 图 4 已删;实为 **8 组多图轮播**(17 个 radio)| 改正 |
| 35 处灯箱 | **36 处** | 改正 |
| 八个 KPI | **6 个** | 改正 |
| 13 张主配图(图 0–图 12)| **10 张(图 0–图 9)** | 改正 |
| 动效表含「交付接力对比 图 10」 | `.relay` 类实测 **0 命中**(随图 10 一起删了)| 删除该行 |
| 「不使用图片文件……**因此页面没有任何图片请求**」 | 站上有 **33 张真实截图 / 106 个 `<img>`** | ❗**改正**:这句在加截图之前是对的,现在是错的;已改为「不使用图标字体/canvas/SVG」并补注截图不属于 CSS 配图 |
| `images/README.md` 清单「位置」列写「第 4 屏 管理端 / 第 6 屏 App 十二格」 | 十一屏结构已不存在 | 按七屏改写,并**追加实测生成的〈36 个图位现址总表〉**作为权威口径 |

## 五、工作台大扫除

- **2a 过时指引清除**:删掉「待用电脑浏览器重截 C01–C07、C10」整块
  (站上这些主题的图位早已由现有桌面截图覆盖,C 类特写占位从未加进 `index.html`);
  同时删掉已过期的〈进度速查〉〈暂缓入库的三张〉〈关于你放进来的 18 张图〉〈D 类重截清单〉四块,
  统一由**置顶的「当前待办」板**承担。`B2` / `D3` 引用全文归零。
- **2b 重复夹清空(先 md5 实证)**:

  | 文件夹 | md5 | 判定 |
  |---|---|---|
  | `A2-客人交付页` | `6fbcd942d89aef7825e876dfe041b0db` | 三者**逐字节相同**,与 v17-B 记录 `6fbcd942…` 一致 → 删 |
  | `D1-客人交付页整页` | `6fbcd942d89aef7825e876dfe041b0db` | 同上 → 删 |
  | `D2-客人选片输入区特写` | `6fbcd942d89aef7825e876dfe041b0db` | 同上 → 删 |
  | (参照)`A1-客人选片页` | `5f7f22b4ce65d105cb7e9265cf2749c9` | **md5 不同但画面相同**(同尺寸 1077×2209、不同压缩);已入库为 `guest-photo-select` |

  删除前已逐张打开核对画面确为同一屏。三个 `README.txt` 全部保留,顶部加注
  「(此目标已由 A1 覆盖入库,本夹无需再放图)」。
- **2c 新建 `E4-作品提交记录`**(可选夹,七段式 README):目标 `app-work-review`,
  要求用**提交过作品的账号**截到至少一条历史记录;竖屏 390×844;
  明确写了「没有也不影响网站,现挂的空态图是真实界面」。
- **2d 置顶「当前待办」板**:🔴 E1 / 🟡 E2、E3 / ⚪ E0、E4 / ✅ 其余,
  与站上**实测仅剩的 3 个虚线空位**(`console-orders-list`、`app-income-bill`、`app-magic-link`)完全对应。
  入库对照表已追加 E4 行,并把已入库项逐行标注。

## 六、验证(1280 + 真 390,`details` 全展开)

| 指标 | 1280 | 真 390 | 预期 |
|---|---|---|---|
| 图位 | 36 | 36 | 36 ✓ |
| 有图(去重)/ 空位 | 33 / 3 | 33 / 3 | 33 / 3 ✓ |
| 破图(实际渲染)| 0 | 0 | 0 ✓ |
| 横滚 | false | false | false ✓ |
| 内部锚点 / 失效 | 25 / 0 | 25 / 0 | 25 / 0 ✓ |
| `<script>` | 0 | 0 | 0 ✓ |
| 图号顺序 | 0→9 连续 | 0→9 连续 | 连续且=浏览顺序 ✓ |
| 影片 | 2 部 | 2 部 | 2 ✓ |
| 「各约 14 秒」 | 在场 | 在场 | 在场 ✓ |

新图号 **10 处逐一 grep,命中数全部为 1**。
`prefers-reduced-motion: reduce` 强制渲染:图位 36 / 有图 33 / 空位 3 与常态**逐项相同**,
`getAnimations()` 归 0,两部静态分镜各 4 格齐全,横滚 false —— **零信息丢失**。
