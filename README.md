# 产品作品集 · 静态站

一个产品经理作品集单页,介绍「榴档期 · 旅拍自动化调度管理系统」这套真实运行的生产系统——它给谁用、两个端怎么分工、一条订单怎么走完、以及几个关键的产品决策。

## 这是什么

我在日本做旅拍业务,自建了一套覆盖「接单 → 建单 → 派单 → 拍摄 → 交付 → 结算」全链路的业务系统,由三个端协同:运营总控台(管理端)、摄影师移动端「榴档期 App」(iOS 已上架 App Store)、客人免登录交付页。

站点按**先总后分**组织:

- 先说给谁用(四类使用者),再说系统由哪两个入口构成
- 用一张「一条订单的完整旅程」把全站串起来,后面每一块能力都挂在这条线的某一段上
- 十二个能力板块**按归属分成三组**(管理端专属 / 两端共用 / 摄影师端专属),每张卡片可点开看图文详情
- 榴档期 App 的十二项差异化做成**图标 + 一句话**的网格,想看完整论证再点开
- 密度控制:九项机制留三条、七条优势留三条、四个决策只露标题与一句痛点一句结果,其余默认折叠但一条没删

## 页面结构(十屏)

```
 1. Hero          Logo 占位 + 品牌名 + 四个核心数字
 2. 适用场景      四类使用者各自拿到什么(配图1)
 3. 系统构成      两端关系(配图2)+ 一条订单的完整旅程(配图3)★全站地图
 4. 管理端        十二块能力分三组 · 建单流程对比(配图5)· 三端业务流(配图4,默认折叠)
 5. 派单调度      派单模式对比(配图6)+ 三条关键机制(另六条折叠)+ 抢单动线(配图7)
 6. 榴档期 App    三块主界面 + 抢单大厅示意(配图8)+ 十二项差异化图标网格
 7. 模式优势      八环节对比表 + 交付交接对比(配图9)+ 三条优势(另四条折叠)
 8. 自动化与 AI   人工介入次数对比(配图10)+ 自动化清单 + AI 边界
 9. 产品决策      四个案例(默认折叠,含事故影响面图解 配图11)
10. 如何交付      无研发团队的人机协作方法论
```

顶部导航 7 项:适用场景 / 系统构成 / 管理端 / 派单调度 / 榴档期 App / 模式优势 / 决策与交付。

## 配色:暖色琥珀

深色底 + 琥珀橙强调色,全部走 CSS 变量,改一处即可全站生效:

```
--bg #12100C   --bg-alt #16130E   --bg-deep #0D0B08
--accent #E8A33D   --accent-2 #F2BC63          琥珀主色 / 浅琥珀
--warn #C97B4A     --photo #A0AB5E             暖砖红(客人)/ 暖橄榄(摄影师端)
```

三个角色色在流程图与标签上通用:管理端=琥珀,摄影师端=橄榄,客人=砖红。

## 可展开的图文详情

全站共 30 处原生 `<details>`,零 JavaScript:

- 十一张管理端能力卡(摄影师端 App 那块改为指向专章的指引卡,不重复展开)
- 十二格 App 差异化能力
- 四个产品决策
- 两处「更多机制 / 更多优势」折叠区、一处「三端业务流」折叠图

整张卡片任意位置都可点击(图标、标题、描述、要点都在 `summary` 内且不拦截点击),右上角 `+` / `−` 指示,底部有琥珀色「点击展开图文详情」提示;键盘 Tab + 回车同样可用。

## 二十三张配图全部由 CSS 绘制

11 张主配图 + 12 张板块机制图 + 12 个板块图标 + 12 个差异化图标 + 4 个角色图标,全部用 `div/span` + `flex/grid` + `border` + `border-radius` + 伪元素实现。不使用图片文件、图标字体、canvas 或 SVG,因此页面没有任何图片请求。

## 如何替换 Logo

导航栏与 Hero 各留了一个 Logo 占位(`.logoslot` 36×36、`.logoslot.big` 72×72,虚线框内写着 LOGO):

1. 把 logo 文件放进仓库根目录 `images/logo.png`(建议 512×512 透明底 PNG,深色底上要看得清)。
2. 把两处占位替换成 `img`:

```html
<!-- 替换前 -->
<span class="logoslot" aria-hidden="true">LOGO</span>
<!-- 替换后 -->
<img class="logoimg" src="images/logo.png" alt="榴档期">
```

3. 在 `style.css` 末尾补上尺寸:

```css
.logoimg      { display: block; width: 36px; height: 36px; border-radius: 9px; object-fit: contain; }
.logoimg.big  { width: 72px; height: 72px; border-radius: 16px; margin: 0 0 22px; }
```

Hero 那个记得加 `class="logoimg big"`。

## 如何替换截图占位

页面里预留了 **27 处**真实截图占位区(虚线框):4 处主线占位 + 23 处卡片内占位(十一张管理端能力卡 + 十二格 App 差异化)。截好图直接替换即可。

**四处主线占位(各屏正文中)**

| 位置 | 图名 | 建议尺寸 | 打码要求 |
| --- | --- | --- | --- |
| 第 4 屏 · 建单对比之后 | 总控台订单列表页 | 1600×900 | 客人姓名、电话、金额 |
| 第 5 屏 · 派单调度 | 发起抢单弹窗 | 1200×900 | 客人信息 |
| 第 6 屏 · 摄影师端 | 抢单大厅真机截图 | 750×1334 | 需同时出现可抢卡与灰卡 |
| 第 7 屏 · 模式优势 | 客人交付页 | 1200×900 | 客人姓名与金额 |

**十一处管理端板块占位(展开某张卡片后可见)**

| 板块 | 图名 | 建议尺寸 | 打码要求 |
| --- | --- | --- | --- |
| 派单调度 | 发起抢单弹窗 | 1200×900 | 客人信息与报酬金额 |
| 订单管理 | 订单编辑页 | 1400×900 | 客人姓名、电话、金额 |
| 订单公开页 | 客人选片界面 | 1200×900 | 客人姓名与订单金额 |
| 邮件自动化 | 邮件模板管理页 | 1400×900 | 收件人邮箱与客人姓名 |
| 推送体系 | 推送模板中心 | 1400×900 | 模板示例中的客人姓名与金额 |
| 气象与安全预警 | 预警详情弹层 | 1200×900 | 关联订单的客人信息 |
| 交付与精修 | 交付进度页 | 1200×900 | 客人姓名、网盘链接与提取码 |
| 结算体系 | 结算中心报表 | 1400×900 | 金额、摄影师姓名与账户信息 |
| 人员与评分 | 摄影师档案页 | 1200×900 | 姓名、电话与身份信息 |
| 监控告警 | AI 紧急看板 | 1400×900 | 订单号与客人信息 |
| AI 能力 | 订单文本解析界面 | 1400×900 | 原文中的客人姓名、电话、金额 |

**十二处 App 差异化占位(展开某一格后可见)**

| 格子 | 图名 | 建议尺寸 | 打码要求 |
| --- | --- | --- | --- |
| 档期即接单资格 | 档期填报页 | 750×1334 | 摄影师姓名 |
| 抢单大厅 | 抢单大厅列表 | 750×1334 | 集合点与报酬金额 |
| 灰卡也看得见 | 灰卡说明弹窗 | 750×1334 | 订单编号 |
| 评分决定谁先看到 | 评分档位设置弹窗 | 1200×900 | 摄影师姓名与评分 |
| 急单快速通道 | 急单推送通知 | 750×1334 | 客人信息 |
| 推送文案会说话 | 推送文案模板列表 | 1400×900 | 模板中的客人姓名 |
| 天气直接送到手上 | 日历天气底色页 | 750×1334 | 摄影师姓名 |
| 收入自己查 | 个人收入账单页 | 750×1334 | 金额与姓名 |
| 作品提交有回音 | 作品审核结果页 | 750×1334 | 网盘链接与提取码 |
| 自注册自动转正 | 自注册资料页 | 750×1334 | 姓名、电话与证件信息 |
| 免注册长效登录 | 专属链接进入首屏 | 750×1334 | 链接中的令牌串 |
| 版本自动感知 | 新版本更新提示 | 750×1334 | 界面上的摄影师昵称 |

替换步骤:

1. **先打码**——截图里的客人姓名、电话、邮箱、金额、订单号等敏感信息必须先处理干净再入库。图片一旦提交到 Git 并推上 GitHub,历史记录里就会一直存在,删掉当前版本也没用。
2. 在仓库根目录新建 `images/` 文件夹,把处理好的图片放进去(例如 `images/orders.png`)。
3. 在 `index.html` 里找到对应的占位块,整块替换成 `img` 标签:

```html
<!-- 替换前 -->
<div class="shot">
  <p class="shot-name">总控台订单列表页截图</p>
  <p class="shot-req">建议 1600×900 ｜ 需打码:客人姓名、电话、金额</p>
</div>

<!-- 替换后 -->
<img class="shotimg" src="images/orders.png" alt="总控台订单列表页">
```

4. 如果想给图片加上和占位块一致的圆角与边框,在 `style.css` 末尾补一条:

```css
.shotimg { display: block; width: 100%; margin-top: 28px;
           border: 1px solid var(--line); border-radius: 12px; }
```

板块内的十二处占位(`class="shot shot-in"`)替换方式完全相同,只是尺寸更小,建议给 `img` 加上 `margin-top: 16px`。

占位块不替换也不影响观感——它本身是按设计元素处理的,留着即可。

## 本地预览

不需要构建工具、不需要安装依赖:

```bash
open index.html                 # 直接打开
python3 -m http.server 8000     # 或起本地服务,访问 http://localhost:8000
```

## 技术说明

```
index.html    单页全部内容(十屏 + 二十三张 CSS 配图 + 二十八个 CSS 图标)
style.css     暖色深色样式,响应式断点 1024px / 768px,含打印浅色适配
.nojekyll     关闭 GitHub Pages 的 Jekyll 处理
```

- 零 JavaScript:平滑滚动由 CSS `scroll-behavior` 实现,板块展开由原生 `<details>` 实现
- 零外部资源:系统字体栈,无图片素材
- 支持打印导出 PDF

## 数据说明

页面中的业务数字均取自 2026-08 生产环境的只读查询,未做估算或推演。两处例外已在页面上明确标注口径:

- 建单流程对比中的耗时与人力,是 300 单规模下的实际运营口径;其中「2–4 分钟」只指**建单环节本身**,与派单侧的接单时长中位数 2.0 分钟是两个独立指标,不叠加。
- 人工介入次数对比图中的「≈ 15 次」与「≈ 4 次」为**示意口径**,按主要操作节点计数,用于体现结构差异,不是精确统计。

## 部署

托管于 GitHub Pages(main 分支根目录)。
